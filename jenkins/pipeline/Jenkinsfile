node {
  stage('Build') {
    openshiftBuild(buildConfig: 'exntu-test-showcase', showBuildLogs: 'true')
  }
  stage('Unit Test') {
    sh 'mvn test'
  }
  stage('Deploy') {
    openshiftDeploy(deploymentConfig: 'exntu-test-showcase')
  }
}